name: 'dbt_databricks_project'
version: '1.0.0'
config-version: 2

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target" 
clean-targets:         
  - "target"
  - "dbt_packages"

models:
  dbt_databricks_project:
    raw:
      +materialized: table
      +schema: raw
    staging:
      +materialized: table
      +schema: staging
    core:
      +materialized: table
      +schema: core
    mart:
      +materialized: table
      +schema: mart


seeds:
  dbt_databricks_project:
    state_abbreviations:
      +schema: ref  
